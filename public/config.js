// Auto-generated — API backend URL
// When served from Vercel, this points to the Cloudflare tunnel
// When served locally, leave empty for same-origin
if (location.hostname !== 'localhost' && location.hostname !== '127.0.0.1') {
  window.__API_BASE = 'https://increased-currency-notify-utc.trycloudflare.com';
}
